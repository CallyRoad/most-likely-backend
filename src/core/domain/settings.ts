export enum SettingsGame {
    MIN_PLAYERS = 2,
    MIN_QUESTIONS = 5,
}

export enum SettingsId {
    USER_ID = 5,
    GAME_ID = 8
}

export enum RegEx {
    PASSWORD="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$"
}